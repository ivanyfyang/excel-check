# 考勤审核工具

纯前端考勤数据审核工具，上传考勤表与加班申请单后在浏览器中自动检测数据问题。

## 技术栈

- pnpm + Vite 构建
- xlsx (SheetJS) 解析 Excel
- 纯 vanilla JS，无框架

## 项目结构

```
index.html          Vite 入口 HTML
src/
  main.js           入口，事件绑定与文件读取
  parser.js         Excel 解析（考勤表 + 加班申请单）
  audit.js          审核规则引擎（所有检测逻辑在这里）
  render.js         DOM 渲染
  style.css         样式
data/               测试用原始数据（不参与构建，已 gitignore）
dist/               构建输出
```

## 常用命令

```bash
pnpm dev            # 开发服务器
pnpm build          # 构建到 dist/
pnpm preview        # 预览构建产物
```

## Excel 表结构

### 考勤表

每个 sheet 对应一个考勤周期，sheet 名格式 `YYYYMMDD-YYYYMMDD`。

- Row 0: 标题行
- Row 1: 列头（店铺 / 当班人员 / 打卡日报 / 星期名...）
- Row 2: 日期行 + 加班子列头（节假日加班 / 工作日加班）
- Row 3 起，每 4 行一位员工：
  - 行 1: 班次（含姓名、店铺、汇总数据）
  - 行 2: 上班打卡时间
  - 行 3: 下班打卡时间
  - 行 4: 实际出勤小时数

列布局（0-indexed）：

| 列号 | 内容 |
|------|------|
| 0 | 店铺（仅首位员工行有值，后续员工继承） |
| 1 | 姓名 |
| 2 | 行类型标签（班次 / 上班打卡时间 / 下班打卡时间 / 实际出勤小时数） |
| 3-33 | 31 天每日数据 |
| 34 | 应出勤总工时 |
| 35 | 实际出勤总工时 |
| 36 | 事假工时 |
| 37 | 病假工时 |
| 38 | 年假工时 |
| 39 | 婚假工时 |
| 40 | 产假工时 |
| 41 | 育儿假工时 |
| 42 | 丧假工时 |
| 43 | 其他 |
| 44 | 节假日加班 |
| 45 | 工作日加班 |

### 加班申请单

单 sheet，标准表头，关键列：

- `申请人`：员工姓名（与考勤表姓名匹配）
- `加班日期`：日期
- `工作日类型`：`普通工作日` / `周末` / `法定节假日`
- `加班总时长`：格式如 `11.5小时`
- `当前审批状态`：`已通过` / `审批中` / `已驳回` / `已撤销` / `通过后撤销`

## 审核规则

所有规则实现在 `src/audit.js` 的 `audit()` 函数中。

### 规则 1：等式平衡检查（severity: high）

**公式**：`实际出勤 - 应出勤 = (节假日加班 + 工作日加班) - 总假期`

即：多出的工时应等于加班扣除请假后的净值。如果不等，说明工时记录有矛盾。

- 容差：0.1h

### 规则 2：加班总时长一致性（severity: high/medium）

**对比**：考勤表中 `节假日加班 + 工作日加班` vs 加班申请单中该员工所有 `已通过` 记录的时长之和。

- 仅统计加班日期在当前考勤周期内且状态为"已通过"的申请
- 考勤表有加班但申请单无任何已通过记录时标记为 high
- 容差：0.1h

### 规则 3：工作日加班分类一致性（severity: medium）

**对比**：考勤表 `工作日加班` vs 加班申请单中 `工作日类型=普通工作日` 且 `已通过` 的时长之和。

- 容差：0.1h

### 规则 4：节假日加班分类一致性（severity: medium）

**对比**：考勤表 `节假日加班` vs 加班申请单中 `工作日类型=周末 或 法定节假日` 且 `已通过` 的时长之和。

- 周末加班和法定节假日加班合并对比考勤表的节假日加班字段
- 容差：0.1h

### 规则 5：带薪假期工时异常（severity: high）

**前提**：仅当带薪假期工时 > 0 时触发检查。

假期分类：
- **带薪假期**：年假、婚假、产假、育儿假、丧假、其他
- **扣薪假期**：事假、病假（旷工）

**公式**：`实际出勤 + 带薪假期 = 应出勤 + 加班`

如果左侧 > 右侧，说明带薪假期当天可能重复计入了实际出勤工时（员工请了带薪假，但当天仍然记录了出勤小时数）。

- 容差：0.1h

### 规则 6：未审批加班提醒（severity: medium）

检测加班申请单中状态为 `审批中` 的记录。提醒有多少条未审批及总时长。

## 加班申请单状态过滤

| 状态 | 归类 |
|------|------|
| `已通过` | 有效加班，参与规则 2/3/4 对比 |
| `审批中` | 触发规则 6 提醒 |
| `已驳回` / `已撤销` / `通过后撤销` | 仅展示，不参与计算 |

## 日期范围

从考勤表 sheet 名解析（格式 `YYYYMMDD-YYYYMMDD`），加班申请单中超出此范围的记录不参与本期审核。
